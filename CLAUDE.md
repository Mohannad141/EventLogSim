# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — it contains only the default GitLab project README template and has no source code, build system, tests, or dependencies yet. The remote is `https://git.inf.uni-bayreuth.de/bt727270/praktikum.git`.

When the project gains real content, this file should be updated with:
- Build, lint, test, and run commands (including how to run a single test)
- High-level architecture notes that span multiple files

Until then, there are no project-specific commands or architecture to document. Do not invent build/test commands; ask the user about the intended stack before scaffolding.
